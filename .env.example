# MongoDB connection string
# This file provides an example of environment variables needed for the script.
# Copy this file to .env and fill in your actual MongoDB URI.

# Example for a local MongoDB instance:
# MONGODB_URI="mongodb://localhost:27017/"

# Example for a MongoDB Atlas connection string (replace <username>, <password>, <cluster-url>, and <database_name>):
# MONGODB_URI="mongodb+srv://<username>:<password>@<cluster-url>/<database_name>?retryWrites=true&w=majority"

MONGODB_URI=""

# Coinbase Exchange API Configuration (Optional)
# These variables allow you to customize the data fetched from the Coinbase Exchange API.
# If not set, the script will use its internal default values.

# COINBASE_PRODUCT_ID: The trading pair to fetch from the Coinbase Exchange API (e.g., "BTC-USD", "ETH-USD").
# Defaults to "BTC-USD" in the script if this variable is not set.
# Example: COINBASE_PRODUCT_ID="ETH-USD"
COINBASE_PRODUCT_ID=""

# COINBASE_GRANULARITY_SECONDS: The data point interval (candle duration) in seconds for Coinbase Exchange API data.
# Supported values by the API include: 60 (1 minute), 300 (5 minutes), 900 (15 minutes), 
# 1800 (30 minutes), 3600 (1 hour), 7200 (2 hours), 21600 (6 hours), 86400 (1 day).
# The script defaults to 60 (1 minute) if this variable is not set or if an invalid value is provided.
# Example: COINBASE_GRANULARITY_SECONDS="300"
COINBASE_GRANULARITY_SECONDS=""
